name: SCUS_944.88
sha1: 3030aa271c0a4022fc69ce09d76a6bc75e69a32a
options:
  basename: scus_944.88
  target_path: orig_bin/scus_94488/SCUS_944.88
  base_path: .
  platform: psx
  compiler: GCC

  # asm_path: asm
  # src_path: src
  # build_path: build
  # create_asm_dependencies: True

  ld_script_path: scus_944.88.ld

  find_file_boundaries: False
  gp_value: 0x00000000

  o_as_suffix: True
  use_legacy_include_asm: False

  asm_function_macro: glabel
  asm_jtbl_label_macro: jlabel
  asm_data_macro: dlabel

  section_order: [".rodata", ".text", ".data", ".bss"]
  # auto_all_sections: [".data", ".rodata", ".bss"]

  symbol_addrs_path:
    - symbol_addrs.txt
  reloc_addrs_path:
    - reloc_addrs.txt

  # undefined_funcs_auto_path: undefined_funcs_auto.txt
  # undefined_syms_auto_path: undefined_syms_auto.txt

  extensions_path: tools/splat_ext

  subalign: 2

  string_encoding: ASCII
  data_string_encoding: ASCII
  rodata_string_guesser_level: 2
  data_string_guesser_level: 2
segments:
  - name: header
    type: header
    start: 0x0

  - name: main
    type: code
    start: 0x800
    vram: 0x80010000
    # bss_size: Please fill out this value when you figure out the bss size
    subsegments:
      - [0x800, asm, part1] # estimated
      - [0x2538, data, strings] # estimated
      - [0x25F8, bin, notice.tim.gz]
      - [0x23A2C, bin, logo-scea.tim.gz]
      - [0x245A0, data, strings2]
      - [0x245E4, pad, nops]
      - [0x4DE00, asm, start]
      - [0x4DEE0, asm, part2]
  - [0x99800]
